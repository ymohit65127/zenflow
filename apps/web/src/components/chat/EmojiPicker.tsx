"use client";

/**
 * EmojiPicker — a lightweight popover grid of ~50 common emojis.
 * Re-exports the existing emoji-picker component to avoid duplication.
 */

export { EmojiPicker, InlineEmojiGrid } from './emoji-picker';
